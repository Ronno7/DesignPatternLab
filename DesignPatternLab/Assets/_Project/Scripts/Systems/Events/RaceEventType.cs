namespace DesignPatternLab.Systems.Events
{
    // Baron's Chapter 6 race events. This demo uses COUNTDOWN, START, and STOP.
    public enum RaceEventType
    {
        COUNTDOWN, START, RESTART, PAUSE, STOP, FINISH, QUIT
    }
}
